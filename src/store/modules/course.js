import { getField, updateField} from 'vuex-map-fields';

const state = {
    course : {},
}

const getters = {
    getField,
}

const mutations = {
    updateField,
}

const actions = {

}

export default {
    state,
    getters,
    mutations,
    actions
}