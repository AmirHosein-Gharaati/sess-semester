import { getField, updateField} from 'vuex-map-fields';

const state = {
    courseId : null
}

const getters = {
    getField,
}

const mutations = {
    updateField,
}

const actions = {

}

export default {
    state,
    getters,
    mutations,
    actions
}